schemaVersion: treeseed.guarantee/v1
id: guarantee.deployment.local-platform.run-exact-seed-platform.742
journeyIndex: 742
type: deployment
subtype: local-platform
journey: Run an exact seed platform
ownerPackage: "@treeseed/cli"
surface: cli
summary: Configure when required, compose the exact selected seed set, start the agents and platform capacity providers together when both seeds are selected, verify both, detach, preserve reboot supervision, and defer background mutation while operator workflows are active.
status: planned
dependencies: { journeys: [], guarantees: [] }
actors: { allowed: [platform_admin], forbidden: [unauthorized_user] }
devices: { required: [desktop_chromium], optional: [] }
gates: [future]
preconditions: { fixtures: [integrated_treeseed_project], notes: [] }
api: { required: false, verifierRefs: [] }
content: { required: false, verifierRefs: [] }
audit: { required: true, verifierRefs: [cli.deployment.local-platform.workflow-deferral] }
negativeCases:
  - { id: destructive-removal-without-confirmation, actor: platform_admin, verifierRefs: [] }
  - { id: active-workflow-interference, actor: platform_admin, verifierRefs: [cli.deployment.local-platform.workflow-deferral] }
evidence: { required: [guarantee_report] }
notes: ["The run plan for treeseed agents platform must select both class-scoped provider units and their separate Compose projects. The detached supervisor must yield to active save, update, stage, and release workflow locks before every mutating phase. Activate after detached supervisor and reboot acceptance evidence are repeatable."]
